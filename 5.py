class Solution:
    def fun(self, nums: List[int])->int:
        a=0
        b=len(n)

        for index1 in range(b):
            for index2 in ramge(index1+1,b):
                if n[index1]==nindex2]:
                    a+=1
        return a